# Notion DB 스키마 — 유튜브 라이브러리

Notion에서 새 Database(Table) 생성 후 아래 필드로 구성하세요. (필드명은 스크립트의 `NOTION_PROPERTY_MAP`과 정확히 일치해야 합니다)

| 필드명 | 타입 | 설명 |
|---|---|---|
| Title | Title | 영상 제목 (자동 채움) |
| URL | URL | 원본 유튜브 링크 (중복 체크 키) |
| 채널명 | Text | 유튜브 채널 이름 |
| 게시일 | Date | 영상 업로드 날짜 |
| 수집일 | Date | 텔레그램에 공유된(수집된) 날짜 (자동, created_time 대체 가능) |
| 요약 | Text | Claude가 생성한 3~5문장 요약 |
| 카테고리 | Select (단일 선택) | 일반시황 / 반도체 / 제약·바이오 / 이차전지 / 방산 / AI인프라 / 가상자산 / 전략·백테스트 / 기타 — "폴더" 역할 |
| 태그 | Multi-select | 카테고리를 보완하는 세부 키워드(복수 가능) — 예: 반도체+HBM, 이차전지+양극재 |
| 활용여부 | Multi-select | 월간자료 / 매매봇 / 미정 |
| 우선순위 | Select | 상 / 중 / 하 (선택, Claude가 판단해 자동 태깅하거나 나중에 수동 조정) |
| 원문자막 | Text (또는 별도 하위 페이지 본문) | 전체 자막 원문 — 길면 Notion 페이지 본문에 넣는 게 나음 (2000자 제한 있는 rich_text 필드 대신) |

## "폴더별 정리"는 어떻게 구현하나

Notion DB에는 실제 폴더 개념이 없지만, **카테고리(Select) + Board 뷰**로 폴더처럼 쓸 수 있습니다.

1. 영상이 저장될 때 Claude가 자막을 읽고 `카테고리`(단일) + `태그`(세부 키워드, 복수)를 자동으로 채움
   — "이건 일반 시황", "이건 반도체" 같은 판단을 스크립트가 자동으로 함
2. Notion DB에서 뷰(View)를 하나 추가 → **Board 뷰** 선택 → Group by = `카테고리`
   → 카테고리별로 칼럼(=폴더처럼 보이는 구획)이 자동 생성되고, 영상이 해당 칼럼에 자동으로 쌓임
3. 필요하면 카테고리별로 **필터된 Linked View**를 별도 페이지로 만들어서
   "반도체" 페이지, "이차전지" 페이지처럼 진짜 폴더 느낌으로 분리할 수도 있음
   (같은 DB를 소스로 하는 뷰이므로 데이터 중복 없이 여러 곳에서 열람 가능)

카테고리가 애매한 영상(여러 주제 혼합)은 가장 비중 높은 주제 하나로 분류하고,
나머지는 `태그`에 남겨서 놓치지 않게 했습니다.

## 설계 메모

- **중복 방지**: 스크립트가 저장 전에 `URL` 필드로 기존 페이지를 쿼리해서 이미 있으면 skip
- **카테고리는 폴더 역할(단일 선택), 태그는 보완용 세부 키워드(복수 선택)** — 이렇게 나눠야 Board 뷰로 깔끔하게 폴더 정리가 됨. JK님 관심분야(반도체 소부장, 제약, 이차전지, 방산, AI인프라, 가상자산) 기준으로 초기 옵션 세팅해뒀습니다
- **원문 자막은 DB 속성(property)이 아니라 페이지 본문(children blocks)에 넣는 걸 권장** — Notion rich_text 속성은 길이 제한이 있고, 본문에 넣으면 나중에 Notion AI나 검색으로 원문 그대로 열람 가능
- 매매봇은 장개시 전/장종료 전 하루 2번만 참조하므로 Notion API를 그대로 직접 쿼리해도 rate limit 문제 없음 — 별도 벡터DB 불필요. `카테고리`+`활용여부(매매봇)` 필터로 조회하는 쿼리 하나면 충분
